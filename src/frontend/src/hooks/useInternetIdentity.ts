/**
 * Re-export useInternetIdentity from the Caffeine core-infrastructure package.
 * This file exists as a convenience alias so components can import from @/hooks/useInternetIdentity.
 */
export { useInternetIdentity } from "@caffeineai/core-infrastructure";
